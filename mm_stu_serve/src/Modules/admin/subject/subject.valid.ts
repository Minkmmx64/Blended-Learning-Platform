import * as Joi from "joi";
import { SubjectCreateDTO, SubjectUpdateDTO } from "./subject.dto"; 

export const SubjectCreateValid = Joi.object<SubjectCreateDTO>({

})

export const SubjectUpdateValid = Joi.object<SubjectUpdateDTO>({

})
